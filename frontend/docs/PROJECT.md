# Project Management

## Current Task
- Login Form data submission and error handling
    - send data with a `POST` request
    - check if response ok or not
    - if ok then redirect to dashboard/profile page
    - set access token in a variable <<
    - if not ok throw an error and catch that error in catch block