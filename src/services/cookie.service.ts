import type { Response } from "express";


const setSessionIdCookie = (sid: string, res: Response) => {
    
}

const setRefreshTokenCookie = (token: string, res: Response) => {

    
}

export default {
    setSessionIdCookie,
    setRefreshTokenCookie,

}